<view class="container">

<view class="welcome">Welcome to WeSchedule!!!</view>
  <!-- <view class="userinfo">
    <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 获取头像昵称 </button>
    <block wx:else>
      <image bindtap="bindAvatarTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
      <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    </block>
  </view>
  <view bindtap="onEntry" class="usermotto">
    <text  class="user-motto">{{motto}}</text>
  </view> -->
</view>
<button wx:if="{{showButton}}" open-type="getUserInfo" bindgetuserinfo="bindGetUserInfo">授权登录</button>
